#include <stdio.h>

int main() {
	int a[10];
	int b;
	int c = 0;
	for (int i = 0; i < 10; i++) {
		scanf("%d", &a[i]);
	}
	scanf("%d", &b);
	for (int j = 0; j < 10; j++) {
		if (a[j] <= (b + 30)) {
			c++;
		}
	}
	printf("%d", b);
	return 0;
}
